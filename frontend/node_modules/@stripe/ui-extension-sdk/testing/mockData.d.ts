import type { ExtensionContextValue } from '../context';
export declare const defaultMockContextProps: ExtensionContextValue;
declare type DeepPartial<T> = Partial<{
    [P in keyof T]: DeepPartial<T[P]>;
}>;
export declare const getMockContextProps: (partialMockContext?: DeepPartial<ExtensionContextValue>) => ExtensionContextValue;
export {};
